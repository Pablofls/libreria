# Solo para la Tarea 4 (interoperabilidad) y para las pruebas que consumen el
# servicio desde un stack distinto. NO se instala en el servidor: el modulo SOAP
# no depende de zeep, esa es justamente la demostracion.
#
#     pip install -r requirements-interop.txt

# 4.3 o superior, no 4.2.1: esa version importa el modulo `cgi`, retirado en
# Python 3.13, y falla al importarse. El rango deja que pip elija la que
# corresponda a la version de Python de cada maquina.
zeep>=4.3.1,<5
