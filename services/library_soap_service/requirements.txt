# Modulo SOAP de clasificacion Cloud.
#
#     python3 -m venv .venv && source .venv/bin/activate
#     pip install -r requirements.txt
#
# Solo tres dependencias, las que autoriza el enunciado. El sobre SOAP, el
# WSDL y los Faults se construyen a mano con xml.etree, que es de la biblioteca
# estandar: no hay Spyne ni Zeep del lado del servidor.
#
# psycopg2-binary 2.9.10 y no 2.9.9: la 2.9.9 no publica rueda para Python 3.13
# e intenta compilar desde fuente.

Flask==3.0.3
psycopg2-binary==2.9.10
python-dotenv==1.0.1
